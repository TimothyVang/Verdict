# VERDICT swarm — distilled patterns

> Living document. The team lead refreshes this at every cleanup by distilling
> the last ~200 lessons from `lessons.jsonl`. Read this in full before starting
> any task.

## What works

*(empty — populated as the swarm runs)*

## What to avoid

*(empty — populated as the swarm runs)*

## Open questions

*(empty — populated as the swarm runs)*

---

Last distilled: never · Lessons consumed: 0
