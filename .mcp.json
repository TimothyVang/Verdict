{
  "_comment": "Safe default MCP config. Air-gap compatible: no write-capable filesystem server, no network fetch, no credentialed GitHub MCP. Use .mcp.cloud.json or .mcp.dual.json explicitly for connected research sessions. See docs/MCP_FRAMEWORK.md.",
  "mcpServers": {
    "sequential-thinking": {
      "_origin": "modelcontextprotocol/servers (MIT)",
      "_purpose": "Structured multi-step reasoning for planner_critique_node CoVe pass. No credentials; no project filesystem access.",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ]
    }
  }
}
